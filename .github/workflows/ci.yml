name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run check

  a2a-tck:
    runs-on: ubuntu-latest
    env:
      A2A_TCK_DIR: /tmp/a2a-tck
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: git clone https://github.com/a2aproject/a2a-tck.git "$A2A_TCK_DIR"
      - run: git checkout "$(bun "$GITHUB_WORKSPACE/scripts/a2a-profile.ts" --revision)"
        working-directory: ${{ env.A2A_TCK_DIR }}
      - run: python3 -m pip install uv
      - run: python3 -m uv sync --directory "$A2A_TCK_DIR"
      - run: bun run test:a2a-tck
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: a2a-conformance-report
          path: artifacts/a2a-tck
          if-no-files-found: error

  release-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - bun-darwin-arm64
          - bun-darwin-x64
          - bun-linux-arm64
          - bun-linux-x64
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - run: bun run release -- --target "${{ matrix.target }}"
      - run: test -s "dist/release/${{ matrix.target }}/acs"
      - run: test -s "dist/release/${{ matrix.target }}/LICENSE"
      - run: test -s "dist/release/${{ matrix.target }}/THIRD_PARTY_NOTICES"
      - run: shasum -a 256 -c SHA256SUMS
        working-directory: dist/release/${{ matrix.target }}
