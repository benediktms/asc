name: Completed isolated ACS verification transfer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'The one-time verification transfer has completed.'
